<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>ARNOLOOG</title>

  <style>
    body {
      background: #000;
      color: #fff;
      margin: 0;
      font-family: Arial, sans-serif;
    }

    header {
      padding: 20px;
      text-align: center;
    }

    header a {
      color: #fff;
      text-decoration: none;
      font-size: 24px;
      letter-spacing: 2px;
      font-weight: bold;
    }

    main {
      padding: 10px;
    }

    /* GRID LAYOUT */
    .grid {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 2px;
    }

    .grid img {
      width: 100%;
      height: 100%;
      aspect-ratio: 1 / 1;
      object-fit: cover;
      display: block;
    }

    /* TRIGGER EFFECT */
    .trigger {
      outline: 2px solid rgba(255,255,255,0.3);
      animation: pulse 2.5s infinite ease-in-out;
    }

    @keyframes pulse {
      0% { outline-color: rgba(255,255,255,0.2); }
      50% { outline-color: rgba(255,255,255,0.7); }
      100% { outline-color: rgba(255,255,255,0.2); }
    }

    /* ROLL PAGE (FULL FOTO’S) */
    .roll-title {
      text-align: center;
      margin-bottom: 20px;
      font-size: 20px;
      letter-spacing: 1px;
    }

    .roll-gallery {
      display: flex;
      flex-direction: column;
      gap: 20px;
      padding-bottom: 50px;
    }

    .roll-photo img {
      width: 100%;
      height: auto;
      display: block;
    }

  </style>
</head>

<body>
  <header>
    <a href="/">ARNOLOOG</a>
  </header>

  <main>
    {{ content | safe }}
  </main>

</body>
</html>
