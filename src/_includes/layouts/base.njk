<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <title>{{ title or site.title }}</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="{{ site.description }}">

    <link rel="stylesheet" href="{{ '/styles.css' | url }}">
  </head>

  <body class="page">
    <header class="site-header">
      <div class="site-header__inner">
        <a href="{{ '/' | url }}" class="logo">ARNOLOOG</a>
        <nav class="nav">
          <a href="{{ '/' | url }}">grid</a>
          {# link naar nieuwste roll (laatste in rollsList) #}
          {% if photos and photos.rollsList and photos.rollsList.length %}
            {% set latestRoll = photos.rollsList | last %}
            <a href="{{ ('/rolls/' ~ latestRoll ~ '/') | url }}">rolls</a>
          {% endif %}
        </nav>
      </div>
    </header>

    <main class="site-main">
      {{ content | safe }}
    </main>

    <footer class="site-footer">
      <p>&copy; {{ "now" | date("yyyy") }} ARNOLOOG – Analog photography.</p>
    </footer>
  </body>
</html>
