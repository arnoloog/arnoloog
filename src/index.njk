---
layout: layouts/base.njk
title: ARNOLOOG
---

<section class="home-grid">
  {% for rollName in photos.rollsList %}
    {% set roll = photos.rolls[rollName] %}

    {% for img in roll.grid | reverse %}
      <img src="/photos/rolls/{{ rollName }}/{{ img }}" loading="lazy">
    {% endfor %}
  {% endfor %}
</section>
